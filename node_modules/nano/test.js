import Nano from './lib/nano.js'
const nano = Nano({ url: process.env.COUCH_URL, headers: {}})

async function main() {
  const response = await nano.info()
  //(await nano.db.listAsStream()).pipe(process.stdout)
  console.log(response)
}

main()
